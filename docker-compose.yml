services:
  db:
    image: postgres:18
    container_name: lms-postgres
    environment:
      POSTGRES_DB: lms
      POSTGRES_USER: lms
      POSTGRES_PASSWORD: lms_dev_password
    ports:
      - "5432:5432"
    volumes:
    - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: